# Task List — Van Sales Hardening

Derived from `reports/2026-07-06_implementation_plan.md`. Execute top-to-bottom:
Phase 0 → Phase 1 → Phase 2 → Phase 3 → Phase 4.

---

## Phase 0 — Quick Wins (parallel, low risk)

- [x] **P0.1** Run `dart fix --apply`, then `flutter analyze` until clean. _(dart fix: nothing to fix. Excluded `test/test_org_sync.dart` — a non-test debug script with hard-coded live Zoho creds, left as-is per user decision — from analysis via `analysis_options.yaml`. `flutter analyze` now reports 0 issues.)_
- [x] **P0.2** Replace `print(` with `debugPrint`/logger across `lib/`. _(21 call sites across `main.dart`, `zoho_api_client.dart`, `sync_worker.dart`, `license_cubit.dart`; added `flutter/foundation.dart` imports where needed. Full structured-logger + sanitization work remains under P2.4.)_
- [x] **P0.3** `const` sweep across widgets. _(Enabled `prefer_const_constructors` + `prefer_const_literals_to_create_immutables` in `analysis_options.yaml`, ran `dart fix --apply` — 17 fixes across 9 files. `flutter analyze` clean.)_
- [x] **P0.4** Apply `SafeArea` consistently on all pages. _(19 page-level Scaffolds audited; 6 already had it, 15 wrapped now. `login_page.dart` wraps only the form content, not the decorative full-bleed gradient background. `flutter analyze` clean, `dart format` applied.)_
- [x] **P0.5** Surface `roundOff` in transaction editors. _(Already present: `sales_invoice_editor_page.dart:432-437` and `sales_order_editor_page.dart:477-482` both show a conditional "Round Off:" row when nonzero. No other transaction type — receipt/expense/return — has a `roundOff` field in its domain model, so no further surface needed here.)_
- [x] **P0.6** Add stock-warning indicators in item search. _(Already present in shared `core/widgets/item_search_sheet.dart:173-184` — trailing "Stock: N" label colored green when >0, red when 0/negative. Feature + sales_order wrapper dialogs both delegate to this shared sheet, so they inherit it.)_
- [x] **P0.7** Make mock mode visible at runtime (indicator). _(Added `ZohoApiClient.isAnyMockModeActive` getter (`zoho_api_client.dart:102-106`, true if creds are placeholders OR either transaction-mock flag is set) and a persistent amber `_MockModeBanner` shown app-wide via the `MaterialApp.builder` in `lib/app.dart`. Currently visible in this build since `_mockTransactions=true`.)_
- [x] **P0.8** Add "Retry Failed" + "Clear Queue" actions in sync UI. _(Added `HiveDatabaseService.clearFailedSyncItems()` (removes only `SyncStatus.failed` items, never touches pending/syncing — avoids discarding unsynced work), threaded through `SyncRepository`/`SyncRepositoryImpl`, new `ClearFailedItemsRequested` event on `SyncBloc`. "Retry Failed" re-fires the existing `TriggerSync` (the worker already retries pending+failed together). Both surfaced as a conditional actions bar in the Sync Queue tab of `masters_sync_page.dart` when `failedCount > 0`. `flutter analyze` clean, `flutter test` all 20 passing.)_
- [x] **P0.9** Ensure `context.org` + `formatCurrency` used everywhere (no hardcoded currency/company). _(Audited ~30 files using currency symbols: all route through `context.org.currencySymbol` (as `cs`) or `org?.currencySymbol` in PDF templates. Company name likewise via `context.org.companyName` / `org?.name`. Remaining literal "Van Sales Pro" strings are either `??` null-safe fallbacks or the app's own pre-auth login-splash branding — not violations. Note: many call sites hand-roll `'$cs${v.toStringAsFixed(2)}'` instead of calling the shared `formatCurrency()` helper in `currency.dart` — that dedup is tracked separately under P3.7.)_

---

## Phase 1 — Critical

- [x] **P1.2** ~~Introduce a `Money` value object for all amounts~~ — descoped by user decision to a narrower precision-safe rounding fix (full `Money`/`package:decimal` migration across domain+editors+PDFs+Hive judged too large/risky for this pass). _(Added `lib/domain/utils/money_math.dart` — `roundMoney()` rounds to 2 decimals at every money-bearing getter, so floating-point drift from chained `fold()` sums (e.g. `0.1 * 30 != 3.0` in raw double) can't accumulate across a document's totals. Applied to `sales_invoice.dart`, `sales_order.dart` (line + document level: subTotal/taxAmount/total/taxTotal/discountTotal/rawTotal/roundOff), `receipt_voucher.dart` (totalAllocated/unallocatedAmount), `expense_entry.dart` (amount), `sales_return.dart` (document total only — the line-item tax/discount bug itself is P1.3). Added a drift-regression test in `domain_totals_test.dart`. `flutter analyze` clean, all 21 tests pass.)_
  - [x] ~~Add `lib/domain/value_objects/money.dart` wrapping `package:decimal`~~ — not done; used a lightweight `roundMoney()` helper instead, see above.
  - [x] Migrate line-item + document totals: `sales_invoice.dart`, `sales_order.dart`, `sales_return.dart`, receipt allocations — done via `roundMoney()`, not a `Money` type.
  - [ ] ~~Keep Hive JSON compatible (store as string / scaled int) + adapter migration~~ — not applicable, no storage format changed (still plain `double`).
  - [ ] ~~Route display through `Money.format`~~ — not applicable; display still goes through `lib/ui/core/utils/currency.dart` as before.
- [x] **P1.3** Fix `SalesReturn` total to include tax & discount. _(`sales_return.dart` `SalesReturnLineItem` now has `subTotal`/`taxAmount`/`discountAmount`/`total` mirroring `InvoiceLineItem`'s subtotal→tax→discount flow, using the original line's tax rate and a **prorated** discount — `discountAmount = (invoiceLineItem.discount / invoiceLineItem.quantity) * returnedQuantity` — so a partial return only credits back its proportional share of the original discount, not the full line discount. Added 2 regression tests (full-qty and partial-qty return) in `domain_totals_test.dart`. PDF template and sync payload already just read `.total`/`.subTotal` fields, so no other call sites needed changes. `flutter analyze` clean, all 23 tests pass.)_
  - [x] Replace `rate * returnedQuantity` at `sales_return.dart:29` with `InvoiceLineItem` logic (subTotal → tax → discount).
  - [x] Confirm returns reconcile with originating invoice and Zoho — verified via unit tests; no live Zoho reconciliation environment available in this session to confirm end-to-end against a real org.
- [x] **P1.4** Domain-level stock rules + negative-stock prevention. _(Added `lib/domain/utils/stock_rules.dart`: `deductStock()` + `InsufficientStockException` — the single invariant "stock can never go below zero," used by both layers. `hive_database_service.dart:saveLocalInvoice` now computes/validates all item stock deltas **before** writing anything (previously it persisted the invoice first, then silently clamped negative stock to 0 — meaning invoice + stock could end up inconsistent); it now throws and writes nothing if any line would go negative. `SalesInvoiceBloc`'s 3 separate ad-hoc stock comparisons (`_onAddOrUpdateLineItem`, `_onAddToCart`, `_onUpdateCartQuantity`) now all call the same `deductStock()` in a try/catch instead of duplicating the `>` check, so UI and persistence can't disagree. Added `test/stock_rules_test.dart` (4 tests) covering the invariant directly. `flutter analyze` clean, all 27 tests pass.)_
  - [x] Move `allowedStock` check out of `SalesInvoiceBloc` into a domain/repository rule.
  - [x] Ensure `saveLocalInvoice` cannot silently floor stock to 0.
  - [x] Single invariant shared by UI validation + persistence.
- [x] **P1.5** Make mock/live behavior runtime-configurable and visible. _(`ZohoApiClient._mockTransactions`/`_mockSalesOrderTransactions` are now mutable instance fields (still default `true`/`false` to preserve current behavior) set via new `updateMockFlags()`, called from `ServerConfigCubit.setConfig()` alongside `updateCredentials()`. Added `mockTransactions`/`mockSalesOrderTransactions` to the `ServerConfig` domain model + Firestore `fromMap`/`toMap`, defaulting to `true`/`false` when absent from an org's existing Firestore doc — so no org silently starts pushing live transactions until someone explicitly sets `mock_transactions: false` remotely (confirmed this default with the user before implementing, since the alternative default risked accidentally going live for existing orgs). Also fixed a staleness bug in the P0.7 banner: it read `isAnyMockModeActive` once at `MaterialApp` build time with no rebuild trigger, so it wouldn't reflect mock flags applied later by `ServerConfigCubit` after login — now wrapped in `BlocBuilder<ServerConfigCubit, ServerConfigState>` so it updates reactively. `flutter analyze` clean, all 27 tests pass.)_
  - [x] Replace compile-time `_mockTransactions` / `_mockSalesOrderTransactions` with server-config value via `ServerConfigCubit`.
  - [x] Persistent "MOCK MODE" indicator in app bar / sync banner — done in P0.7, made reactive here.
- [x] **P1.6** Document + align rounding strategy vs Zoho. _(Wrote `docs/rounding_strategy.md`: documents the subtotal→tax→discount order (tax computed on pre-discount subtotal; Zoho supports both discount-before-tax and discount-after-tax modes, so this needs live-org verification — flagged as an open question, not resolved, since no Zoho sandbox was available this session), the prorated return discount logic from P1.3, the `roundMoney()` drift guard from P1.2, and the whole-unit `roundOff` document-level rounding convention. Tests already exist in `domain_totals_test.dart` and `stock_rules_test.dart` — no new `Money` type was introduced so nothing further to encode there.)_
  - [x] Write down discount-vs-tax order and `roundOff` behavior; verify against Zoho Books — documented; live-org verification flagged as an open item, not resolved (no Zoho sandbox access this session).
  - [x] ~~Encode as tests once `Money` exists~~ — no `Money` type introduced (see P1.2); relevant behavior already covered by existing tests.

---

## Phase 2 — High

- [ ] **P2.1** Skipped by user decision — large structural refactor across 5+ BLoCs with real regression risk and no widget-level test safety net to catch UI breakage. Deferred to a future session; architecture left as-is (works, just not neatly factored).
- [x] **P2.2** Centralize date-filter logic. _(Used a pure shared function instead of a state mixin — a mixin with fields on 5 different `Equatable` state classes would have meant touching every constructor/`copyWith`/`props` list, edging back into the P2.1-style refactor risk the user just deferred. Added `lib/ui/core/utils/date_filter.dart` — `filterByDateRange<T>()`, day-granularity inclusive range filter. Applied to the 5 BLoCs that actually had this duplicated logic: `SalesInvoiceState.filteredInvoices`, `SalesOrderState.filteredOrders`, `ReceiptState.filteredReceipts`, `ExpenseState.filteredExpenses`, `SalesReturnState.filteredReturns` (note: `route_bloc.dart` has no date-filter logic — the plan's "6 BLoCs" count appears to overcount by one). Added `test/date_filter_test.dart` (5 tests). `flutter analyze` clean, all 32 tests pass.)_
- [x] **P2.3** Remove thin per-feature wrapper dialogs. _(Deleted all 4 files: `sales_invoice/widgets/item_line_editor_dialog.dart`, `item_search_dialog.dart`, and their `sales_order/` twins. Inlined the search+edit composition directly into `_openItemSearch`/`_editLineItem` in both `sales_invoice_editor_page.dart` and `sales_order_editor_page.dart`, calling `ItemSearchSheet`/`SharedItemLineEditorDialog` from `core/widgets/` directly with the same per-feature config the wrappers used to hardcode (invoice: default stock-limited; order: `allowUnlimitedQuantity: true`, `title: 'Order Line Item Details'`). Left `sales_return/widgets/return_item_search_dialog.dart` + `return_invoice_selector_dialog.dart` alone — not in the plan's list and not a thin duplicate (real invoice-filtering logic, different dialog). `flutter analyze` clean, all 32 tests pass.)_
  - [x] Delete `sales_invoice/widgets/item_line_editor_dialog.dart`, `item_search_dialog.dart` and `sales_order/` twins.
  - [x] Point call sites at `core/widgets/` (`SharedItemLineEditorDialog`, `ItemSearchSheet`).
- [x] **P2.4** Structured logging + error classification. _(Added `lib/data/services/app_logger.dart` — `AppLogger.{debug,info,warning,error}(tag, message)`, wraps `debugPrint` with level+tag prefixes and regex-based redaction of `Zoho-oauthtoken`/`Bearer`/`client_secret`/`refresh_token`/`access_token` substrings before anything reaches the console. Migrated all 21 P0.2 `debugPrint` call sites in `zoho_api_client.dart` (16), `sync_worker.dart`, `license_cubit.dart` (3), `main.dart` to `AppLogger`. Added `lib/data/services/error_classification.dart` — `classifySyncError()` categorizes a caught error as `transient` (timeouts/connection errors/5xx — worth retrying) or `permanent` (4xx/validation — won't succeed on retry without a data fix). Wired into `SyncWorker`'s catch block: tags the persisted `errorMessage` with `[Retryable]`/`[Needs Attention]`, and the Sync Queue UI (`masters_sync_page.dart`) now shows a matching badge per failed item instead of treating every failure identically — full auto-retry/backoff on the transient category is P2.7. Added `test/app_logger_test.dart` (5 tests, using Flutter's `debugPrint` override hook) and `test/error_classification_test.dart` (7 tests). `flutter analyze` clean, all 44 tests pass.)_
  - [x] Replace all 21 `print()` with a logger; sanitize secrets/PII.
  - [x] Distinguish transient (network) vs permanent (validation) errors; drive retry UX — classification + UI badge done here; automatic backoff retry is P2.7.
- [x] **P2.5** Enforce day-close / cash-closing workflow. _(Added `HiveDatabaseService.hasPendingCashClosingForToday()`: true when there's invoice/receipt/expense activity dated today but no cash-closing record filed for today (a single-slot record — compares its date against "now"). Gated the app's one logout entry point (`masters_sync_page.dart`'s bottom bar — the only place `LogoutRequested` is dispatched anywhere in the app) behind this check: attempting to log out with unreconciled activity shows a blocking dialog directing the user to complete Cash Closing from the Dashboard first, instead of dispatching `LogoutRequested` directly. Did not touch `SessionGateway` itself (per the plan's literal phrasing) — gating at the logout action achieves the same outcome with a much smaller diff; a "next-day" gate would need a redesign of the single-slot closing-record storage to hold day-keyed history, which is out of scope here. Added `test/cash_closing_gate_test.dart` (4 tests). `flutter analyze` clean, all 48 tests pass.)_
- [x] **P2.6** Comprehensive tests on critical paths. _(Used hand-written fakes rather than `mockito` — `SyncWorker`'s dependencies (`HiveDatabaseService`, `ZohoApiClient`) are already tested this way in `sync_all_test.dart`, so stayed consistent with the existing pattern rather than introducing a second mocking approach. **Found and fixed a real production bug while writing these tests**: `syncPendingItems()` captured its `pendingItems` batch once before the loop, so when an earlier item in the same batch was a customer/sales-order whose sync patched a temp id into a later item's payload (`_resolveTempCustomerIdsInQueue`/`_resolveTempOrderIdsInQueue`), the loop kept using the stale pre-patch snapshot for that later item — meaning an invoice created together with its new customer in the same offline session would sync to Zoho with the **wrong temp customer_id** on the very first sync run, exactly the scenario the code's own "CRITICAL" comment claims to prevent. Fixed by re-reading each item's current persisted state from the queue immediately before processing it (`sync_worker.dart`). Also refactored `SyncWorker`'s `Connectivity` dependency to accept an injectable `checkConnectivity` function, since the real `connectivity_plus` `Connectivity` class is a non-subclassable singleton that throws `MissingPluginException` outside a real app (blocking any test of `syncPendingItems` at all). Added `test/sync_id_resolution_test.dart` (4 tests: customer→invoice id resolution — the test that caught the bug, sales-order `_persistOrderZohoId` + `_resolveTempOrderIdsInQueue`, and 2 offline-queue-failure tests verifying `[Retryable]`/`[Needs Attention]` tagging). Full invoice/receipt/return financial round-trip already covered by `domain_totals_test.dart` (P1.2/P1.3); stock adjustment edge cases already covered by `stock_rules_test.dart` (P1.4) — a deeper Hive-integration test of `saveLocalInvoice`'s stock-validation-before-persist ordering wasn't added since it would require a new `hive_test`-style dependency to fake real Hive boxes, judged out of scope for this pass. `flutter analyze` clean, all 52 tests pass.)_
- [x] **P2.7** Auto-retry / backoff for failed sync items + "Retry Failed" / "Clear Queue" sync UI actions. _("Clear Queue" UI action already done in P0.8. Added `retryCount` field to `SyncQueueItem` (+ Hive JSON), exponential backoff (`_backoffDelay`: 30s/1m/2m/4m/8m capped at 30m) and a periodic 60s auto-retry `Timer` to `SyncWorker` (+ `dispose()` to cancel it). `syncPendingItems({forceRetryAll})` now: on the automatic path (connectivity-change listener or the 60s timer) skips `[Needs Attention]` (permanent) failures entirely and skips `[Retryable]` (transient) failures still inside their backoff window; `forceRetryAll: true` (the manual "Retry Failed" button) bypasses both checks. Threaded `forceRetryAll` through `SyncRepository`/`SyncRepositoryImpl.triggerSync()`, the `TriggerSync` event on `SyncBloc` (`_onTriggerSync` now forwards `event.forceRetryAll`), and the "Retry Failed" button in `masters_sync_page.dart` (now dispatches `TriggerSync(forceRetryAll: true)`). Fixed 2 pre-existing `SyncRepository` fakes (`integration_test/app_test.dart`, `test/receipt_bloc_test.dart`) that broke the interface after the signature change. Added 3 backoff-specific tests to `test/sync_id_resolution_test.dart` (not-yet-eligible transient failure is skipped; eligible transient failure is retried and succeeds; a Needs-Attention item is never auto-retried but succeeds under `forceRetryAll`). `flutter analyze` clean (0 issues), `dart format` applied, all 55 tests pass.)_
- [x] **P2.8** Refresh open-invoice snapshot in the receipt flow (avoid stale allocation targets). _(Two receipt entry points read a local Hive cache of open invoices for FIFO allocation — `ReceiptBloc` (full editor page) and `ReceiptPaymentDialog` (dashboard quick dialog) — and both previously took that snapshot once and never refreshed it, so a payment could be FIFO-applied against an invoice already settled (or miss one raised) since the last full master sync. Fixed both: `ReceiptBloc._onSetCustomer` now `await`s `SyncRepository.syncMaster(MasterType.openInvoices)` (best-effort, caught and ignored on failure so it degrades gracefully offline) before computing the FIFO allocation, so selecting a customer pulls a live snapshot first. `ReceiptPaymentDialog` now shows the cached snapshot immediately on open (so it isn't blank offline), then refreshes live via `sl<SyncWorker>().syncMaster(MasterType.openInvoices)` in the background and re-applies the allocation against the fresh data once it lands. Did not touch `_onSetAmount` — re-fetching on every amount keystroke would spam the network for no benefit; the refresh only needs to happen once per customer selection. Added 2 tests to `test/receipt_bloc_test.dart` (asserts `MasterType.openInvoices` is synced on customer selection; asserts allocation still works from the local cache when the live refresh throws/offline). `flutter analyze` clean, all 57 tests pass.)_

**Phase 2 complete** (P2.1 explicitly skipped by user decision; P2.2–P2.8 done and verified).

---

## Phase 3 — Medium

- [ ] **P3.1** Selective rebuilds — add `buildWhen`/`listenWhen` to heavy screens (dashboard, editors).
- [ ] **P3.2** Memoize / incrementally update daily stats (dashboard).
- [ ] **P3.3** `const` pass across widgets; add `Key`s to important widgets.
- [ ] **P3.4** Responsive layer — `LayoutBuilder` + breakpoints; consistent `SafeArea`; better keyboard handling for tablet/landscape.
- [ ] **P3.5** Barcode / QR scanning for items & customers.
- [ ] **P3.6** Persistent offline banner + tap-to-retry on every screen.
- [ ] **P3.7** Dedup PDF currency formatting into one shared helper (`currency.dart`).
- [ ] **P3.8** UX speed — recent/frequent customers & items strip; "repeat last order"; low-stock highlight in item search; numeric keypad for quantity; per-master sync progress + ETA.
- [ ] **P3.9** Extract magic strings to enums — payment modes (`'Cash'`), queue types (`'customer'`/`'invoice'`).
- [ ] **P3.10** Improve error messages with categories; add undo for line-item removal.

---

## Phase 4 — Low / Polish + Future

- [ ] **P4.1** Accessibility audit — `Semantics` labels on key actions; text-scaling support.
- [ ] **P4.2** Localization — `flutter_localizations` + `l10n.yaml`.
- [ ] **P4.3** Advanced inventory — reserved-vs-available, batch/expiry (if domain requires).
- [ ] **P4.4** Skeleton loaders; better animations.
- [ ] **P4.5** Performance profiling pass on large ListViews.
- [ ] **P4.6** Remove dead/legacy cart code paths; drop `ignore_for_file` pragmas where fixable.
- [ ] **P4.7** Certificate pinning; input sanitization; harden first-login license provisioning.
- [ ] **P4.8** Crash reporting / global error boundary.
- [ ] **P4.9** README note documenting sync ID-resolution rules.

---

## Verification (run after each phase)

- [ ] `flutter analyze` clean; `dart fix --apply` leaves no residual fixes.
- [ ] `flutter test` green, with new suites for `Money`, sync ID-resolution, and return totals.
- [ ] Grep confirms zero `print(` in `lib/`.
- [ ] Manual: mock mode shows indicator; switching server config to live clears it and pushes a sales order.
- [ ] End-to-end: invoice → return → receipt for one customer reconciles to the cent against Zoho Books.
